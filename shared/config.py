"""
=============================================================================
A3 v2 配置 —— DeepSeek 主力 + 讯飞星火交叉验证
基于 Multi-Agent-Study-Assistant 二改
=============================================================================
"""
import os

# ============================================================================
# 讯飞星火 API 配置（赛题指定主力模型）
# ============================================================================
SPARK_CONFIG = {
    "APPID": os.getenv("SPARK_APPID", "fb979993"),
    "API_KEY": os.getenv("SPARK_API_KEY", "aa9213dc2668ecf35ffd50776e6130bb"),
    "API_SECRET": os.getenv("SPARK_API_SECRET", "NGU2OWEyNGIyZWE5NmNhMTI1Mzg2YzU0"),
    "MODEL": os.getenv("SPARK_MODEL", "lite"),
    "BASE_URL": "https://spark-api-open.xf-yun.com/v1",
    "TEMPERATURE": 0.7,
    "MAX_TOKENS": 2048,
    "TIMEOUT": 60,
}

# ============================================================================
# DeepSeek API 配置（辅助模型，用于交叉验证）
# ============================================================================
DEEPSEEK_CONFIG = {
    "API_KEY": os.getenv("DEEPSEEK_API_KEY", ""),
    "MODEL": os.getenv("DEEPSEEK_MODEL", "deepseek-chat"),
    "BASE_URL": "https://api.deepseek.com/v1",
    "TEMPERATURE": 0.5,
    "MAX_TOKENS": 4096,
    "TIMEOUT": 60,
    "ENABLED": True,
}

# ============================================================================
# 智谱 GLM API 配置（免费辅助模型，用于交叉验证）
# ============================================================================
GLM_CONFIG = {
    "API_KEY": os.getenv("GLM_API_KEY", ""),
    "MODEL": os.getenv("GLM_MODEL", "glm-4-flash"),
    "BASE_URL": "https://open.bigmodel.cn/api/paas/v4/",
    "TEMPERATURE": 0.5,
    "MAX_TOKENS": 2048,
    "TIMEOUT": 60,
    "ENABLED": bool(os.getenv("GLM_API_KEY", "")),
}

# ============================================================================
# 辅助模型选择（deepseek / glm / none）
# ============================================================================
AUX_MODEL = os.getenv("AUX_MODEL", "deepseek")

# ============================================================================
# 课程知识库路径
# ============================================================================
KB_PATH = os.path.join(os.path.dirname(os.path.dirname(__file__)), "knowledge")

# ============================================================================
# 预设课程
# ============================================================================
COURSES = {
    "人工智能导论": {
        "file": "ai_basics.json",
        "description": "AI基本概念、搜索推理、机器学习、深度学习、NLP和CV",
    },
    "Python程序设计": {
        "file": "python_programming.json",
        "description": "Python语法、数据结构、函数式编程、面向对象、常用库",
    },
    "数据结构与算法": {
        "file": "data_structures.json",
        "description": "线性表、栈队列、树、图、查找排序、分治动规贪心",
    },
    "计算机网络": {
        "file": "computer_networks.json",
        "description": "网络体系结构、TCP/IP协议栈、路由算法、网络安全",
    },
}

# ============================================================================
# 学生画像维度
# ============================================================================
PROFILE_DIMENSIONS = [
    "知识基础",
    "认知风格",
    "学习目标",
    "薄弱环节",
    "学习节奏",
    "兴趣领域",
]

LEARNING_STYLES = {
    "视觉型": "喜欢看图表、视频、动画来理解知识",
    "听觉型": "喜欢听课、讨论、播客来学习",
    "动手型": "喜欢写代码、做实验、动手操作",
    "读写型": "喜欢阅读教材、写笔记、整理文档",
}

KNOWLEDGE_LEVELS = {
    "零基础": "完全没接触过",
    "入门": "了解基本概念，能看懂简单代码",
    "中级": "能独立完成小项目",
    "高级": "能设计和实现复杂系统",
}
